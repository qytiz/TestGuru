  <% if logged_in? %>
      <p class="nav user">Welcome <b><%= current_user.name %></b> Guru <%= link_to 'logout',  logout_path, method: :delete %>
      </p>
    <% else %>
      <p class="nav user">
      <%= link_to 'sign up', signup_path %> <%= link_to 'login', login_path %>
      </p>
  <% end %>